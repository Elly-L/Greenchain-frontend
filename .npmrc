# Always use legacy peer dependency resolution to bypass conflicts
legacy-peer-deps=true

# Always save exact versions when installing packages
save-exact=true

# Use the official npm registry
registry=https://registry.npmjs.org/
